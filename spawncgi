export PHP_FCGI_MAX_REQUESTS=1000
export PHP_FCGI_CHILDREN=5

# Make sure sudo don't complain
sudo true

[ $(pgrep -fl php5 |wc -l) -ne 0 ] && sudo pkill -f php5-cgi
sudo -Eu www-data /usr/bin/php5-cgi -b 127.0.0.1:9000 &
